// Migration script to add ML-related models
// This is a placeholder for the actual migration that Prisma will generate

/**
 * To run this migration:
 * 1. Navigate to the Backend directory
 * 2. Run: npx prisma migrate dev --name add_ml_models
 *
 * This will:
 * - Generate the SQL for the migration
 * - Apply the migration to your database
 * - Generate the Prisma client
 */

// The migration will create the following tables:
// - user_activity_logs
// - user_preferences
// - ml_recommendations
